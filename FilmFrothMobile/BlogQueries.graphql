query AllArticles {
  blogPostCollection(order:date_DESC, limit:10) {
    items{
      style
      path
      title
      date
      preview
      seoPic{
        url
      }
      previewImage{
        url
      }
      author
      content
    }
  }
}
